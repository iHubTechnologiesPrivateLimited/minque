<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_gray3">


   <TextView
       android:layout_width="match_parent"
       android:layout_height="match_parent"
       android:gravity="center"
       android:text="In this hospital bed my medicine is my memories, the good times we shared"
       android:textSize="30dp"
       android:textStyle="bold|italic"
       android:textColor="#fff"
       android:id="@+id/scrolling_text"
       android:maxLines="1"/>

   <!--<TextView-->
       <!--android:text="Single-line text view that scrolls automatically if the text is too long to fit in the widgetSingle-line text view that scrolls automatically if the text is too long to fit in the widgetscrollingTextscrolls automatically if the text is too long to fit in the widgetscrollingTextscrolls automatically if the text is too long to fit in the widgetscrollingText"-->
       <!--android:singleLine="true"-->
       <!--android:ellipsize="marquee"-->
       <!--android:marqueeRepeatLimit ="marquee_forever"-->
       <!--android:focusable="true"-->
       <!--android:focusableInTouchMode="true"-->
       <!--android:scrollHorizontally="true"-->
       <!--android:layout_width="wrap_content"-->
       <!--android:layout_height="wrap_content"-->
       <!--android:id="@+id/scrolling_text"/>-->
</LinearLayout>